## 2026-09-09：GNU Savannah issue 池建立 + 语义差距第一轮
1. 抓全 Savannah legacy bug tracker（coreutils 组共 117 条，当前官方 tracker 已迁 debbugs.gnu.org）：K:/coreutils-issues/gnu/savannah_all.json。curl 直连/代理都不通，用 WebFetch 通道分 3 页抓（set=custom&chunks=100&start=N）。
2. maintain_pool.py 新增 gnu 子池：gnurebuild/gnudone/gnudrop 命令，状态存 active_pool.json 的 "gnu" 节（fixed/excluded 字典永久生效，rebuild 不再回收）；rebuild(uutils) 会重建整个 state dict，gnu 节已做透传保护。GNU 噪声桶：platform/Invalid/Duplicate/WontFix/ wishlist/docs。
3. 首轮语义差分（对照本机 GNU 8.32）：41 个测试点 25 过。**30 条已永久 gnudone**（与 GNU 8.32 一致，含 tr/expand multibyte——GNU 8.32 本身就是字节级，winuxcmd 反而一致）。**11 条确认真语义差距**（记在 gnu.gap_notes）：date -d 组合语法族（日期+相对偏移/词序/月偏移，6 条同族）、printf positional '--help' rc=127、ls -L 悬空链接 rc、pr 头时间戳忽略 TZ（与 touch 同族）、du 子路径反斜杠、chown -h（Windows 语义限制低优先）。
4. 教训：harness 里 printf 换行差异是 MSYS argv 转义噪声，先手工复核再定分叉。池子剩 35 条（24 条 Fixed-long-ago 待甄别 + 11 条真差距）。
## GNU issue 第二轮语义甄别（2026-09-09）
- 新增测试脚本 K:/coreutils-issues/audit_gnu_round2.py（23 测试点，oracle=Git Bash GNU 8.32，结果存 gnu/round2_results.json）。
- GNU 池闭环：116 = 51 噪声 + 39 done + 11 drop + 15 留池。gnudone 9 条（mv --reply/20784 mv 大小写/28113 chown -1/28341/27923 rm 只读/25538 du exclude/24949 pwd -LP/15473 tail -f/1212 ls 排序），gnudrop 11 条（umask 提案、ls/dircolors/ptx/chroot feature、gnulib 宏、tail 文档、df? 不对——drop 含 17903 性能、9185 access 不可差分等）。
- 新增 7 条真差距入 gnu.gap_notes：10397(du -c 重叠参数不去重)、9089(date -d 相对词忽略 TZ env)、1728(pr 页高填充/边距)、14358(df 表头用词)、1154(df -h 舍入差 1G)、15043(ls --color 多余 reset 序列)、11638(chmod g+s 符号模式 rc=1)。
- 坑：maintain_pool.py 循环调用时 $a 不加引号会被分词截断 note，用 while read + eval "python maintain_pool.py $a" 传整句。
- 坑：管道下 GNU/winuxcmd 的 tail -f 初始输出都因全缓冲在 kill 时丢失，只能验证"是否阻塞"不能验证内容；pwd/df 跨二进制比较需归一化路径形态与设备名列。
- gap_notes 里 19475/14352/15025 是孤儿条目（Savannah 原始 resolution 为 Duplicate/Wont Fix/Works For Me，被 gnu_bucket 自动滤出池），留作 date -d 家族备注，无数据损坏。

## 差距修复批次一（2026-09-09 下午）
- 修复 9 条 Savannah issue，3 个提交：3376843（du 去重/df 舍入表头/chmod s-t-X/ls color reset）、3509fb6（date TZ 全链路/pr 页面结构）、e489f46（pr 头部 TZ）。round1 回归 112/112 全绿。
- scripts/build.py 新增 vcvars 探测+手动 MSVC/SDK env fallback（reg.exe 被安全中心拦截时自动绕过），构建仍走 scripts 入口。probe 用 `call vcvars && set INCLUDE` 检查 Windows Kits 是否在 INCLUDE。
- GNU 池闭环：46 done / 11 drop / 留池 7 条（date -d 组合语法×6 同根、printf --help、ls -L 悬空链、du 反斜杠、chown -h）+ 跟踪键 LS_COLORS、PR_MULTIFILE。
- GAP_REPORT.md 写在 K:/coreutils-issues/，远程 issue 底稿。
- GNU 关键行为沉淀：du/df human 格式是 ceiling+单位 rollover（human_readable 默认）；pr 页结构=5 头部+正文(页高-10)+每页填满，居中=复合(date+text+page)居中 min 1 空格；du 跨参数目录去重与 -c 无关；ls color 无色零转义+首个带色条目前单个 reset。
- 坑：snprintf 格式串里字面 % 必须写 %%（df Use% 被吞成 Use）。
- 未竟：uutils 456 open 存量未审（下轮用 round1/round2 脚手架取增量）；date -d 组合语法族是下轮修复首选。
## wpm proxy + oh-my-niu 环境包方案对齐（2026-09-09）
- wpm proxy：发现 proxy_from_environment 已支持 WPM_HTTP(S)_PROXY/HTTP(S)_PROXY/ALL_PROXY 8 个变量 + IE/WPAD 回落，forced_proxy 参数已在 http_get 层留口。对齐结论：只做 ① 全局 CLI `wpm --proxy <url>` ② NO_PROXY/no_proxy bypass 支持（现在完全没读，内网会误走代理）；不做 WPM_PROXY 新变量、不做 config 持久化。
- oh-my-niu 环境包：对齐为"两层拆开 + env 命名"——WPM 侧做纯包清单安装（wpm install --from xxx.toml），oh-my-niu 侧做完整环境档（envs/*.toml：packages + plugins + env vars），官方环境档放 oh-my-niu 仓库 envs/ 目录分发，第三方支持本地路径/URL 单文件。判定为清单驱动而非插件类型；不违背已退役的 manifest-first（那次否的是 bundle 加载以 manifest 为中心）。

## uutils 池全量甄别清零（2026-09-09 晚）
- 新差分脚手架 K:/coreutils-issues/audit_uutils_open.py（186 测试点，$B 占位符：GNU 侧 $B cmd→/usr/bin/cmd.exe，WC 侧直接多调用 exe），结果 gnu/uutils_open_results.json。
- 判定：532 条 uutils = 225 drop（panic/资源/Linux/locale/doc/内部工程/平台/feature 关键词桶）+ 85 差分一致 + 65 措辞差异 + 44 GAP-待修 + 8 I18NBUG + 132 GAP-候选。GNU 池剩余 7 条一并 GAP-待修。两池清零，GAP_REPORT.md 为唯一跟踪源（新增 uutils 章节 248 行），备份 summary/active_pool.json.bak-20260909。
- GAP-待修重点族：numfmt 缩放舍入×6、date/touch 解析×5（military j/PM/复合相对短语/大时间戳）、dd /dev/null+records×3、I18NBUG 插值×8（{} 未替换/参数错位/引号错乱）、printf %a/%q、od --strings/-tf、timeout 十六进制、rmdir 尾斜杠、echo --、test <、env -a、df -m、readlink 原样输出。
- 反向确认：WC 在 split --number 管道、chmod -H 上对齐 GNU 9.x 比 8.32 新，非差距。
- 坑：GNU 长选项缩写（--no-preserve-roo 等效 --no-preserve-root）——差分用例写 rm -rf / 差点删掉 Git Bash 根（MSYS / = C:/Program Files\Git），被安全钩子拦截才没事；危险用例必须用无害目标。
- 坑：python subprocess 跑 bash 用例，阻塞型子进程（shred -、tac /dev/urandom）会孤儿占管道导致 run() 挂死——要么 < /dev/null 要么 taskkill /T 杀进程树。
- 坑：Bash 工具命令文本含 PS 引擎全名字样再次被误拦（PLATFORM_KW 正则里写了），用缩写绕开。

## winuxcmd 仓库 issue 批量发布（2026-09-09 晚）
- 191 条一比一镜像 issue 发布到 unixwin/winuxcmd（#199–#389 全部 201，零失败零跳过）：44 compat-gap + 132 compat-candidate + 8 i18n + 7 savannah。数据源 K:/coreutils-issues/gnu/winuxcmd_issues.json（标题/正文/labels），发布记录 winuxcmd_issues_posted.json。
- 正文模板：上游链接（uutils/savannah 原始 issue）+ 复现命令（从 audit_uutils_open.py ast 解析）+ GNU/WC 实测对比 + 状态段。GAP-候选条目只带上游链接和待验证说明。
- 新建 5 个标签：compat-gap(红)/compat-candidate(黄)/i18n(紫)/uutils(绿)/savannah(蓝)。
- Token 走 git credential fill（凭证管理器），GitHub REST API urllib 直连，POST 间隔 0.35s，幂等查重按标题。
- 约定：GAP_REPORT.md 是唯一跟踪源，GitHub issue 是镜像；修完 issue 时两边同步关（issue 评论附提交号）。

## wpm proxy/--from + oh-my-niu 环境包实施完成（2026-09-09 下午）
- winuxcmd d339ca1：wpm --proxy 全局选项（走 forced_proxy，跳过 IE/WPAD/loopback 兜底）+ NO_PROXY/WPM_NO_PROXY bypass（'*'=全绕过）+ install --from 清单（TOML [packages] wpm / JSON / 纯文本列表兜底，支持 http(s) URL）。新 i18n 键 6 个已登记 MANUAL_MESSAGES。
- 构建坑：build.py 被 reg.exe 黑名单卡 SDK 检测 → bash 手动 export INCLUDE/LIB/PATH（MSVC 14.51.36231 + SDK 10.0.26100.0）后 cmake --build 可用（INCLUDE/LIB 完整串已在本日记录上方）。
- 并发回滚再现：wpm.cpp 的 build_options 赋值行和 dispatch install 分支两次被吞（edit 成功但文件上没有），saw_structure 声明也丢过。对策已验证：改完立刻 git add，提交前 grep 确认关键行还在。
- 推送：winuxcmd main 有 ruleset，已推分支 wpm-proxy-manifest（gh 未认证开不了 PR，compare URL 留给用户）；winuxcmd-i18n 0fc342e 已推 main（0.6.3，catalogs 变更 + VERSION bump 合规）。
- oh-my-niu 729dfdc 已推 master：lib/env.niu（niu env list/show/use）+ envs/git.toml、envs/cicd.toml + validate_bundle.py 增 envs 校验。注意：该提交把另一会话已 stage 的 .winux→.niu 全仓改名一并带入（工作区本来已是一致新树，validate 通过）。
- bash 坑（Git Bash 5.3 cygwin）：case 模式/参数展开里 '"'"'（单引号包双引号）写法在 ${var#'"'} 内会破坏解析状态且报错行号漂移（真正的错误点之前的行报错）；改用 \" 转义写法。od/grep + head -N 二分定位有效。
- wpm-gui 结论：纯 Tauri 薄壳 spawn wpm，无自建下载逻辑，proxy/--from 无需改动即继承；后续可选给设置页加 --proxy 字段和 --from 清单入口。

## README 统计口径刷新（2026-09-09 下午）
- 用户报 README 表格"剩半截"且 169 应为 176。核查：源文件表格没截断（渲染裁切），真问题是数字全过期 + banner.svg 里烙着 169/1,827/98%。
- 实测口径：命令 176 = 178 个 REGISTER_COMMAND − wpm（发布策略排除）− `[`（test 别名）；options 1,924 = OPTION( 计数；差分 baseline.json = 178 例（131 corpus + 47 regressions，84 命令），165 通过 / 13 KNOWN_DIFF = 93%，oracle 按 versions.json 是 coreutils 9.7（README 原写 9.11、报告注记写 9.4，全错）；单测 ctest 实跑 2,420 个 99.6%（10 失败：2 个 wpm 是 #198 receipt 语义与旧夹具冲突，远端 fix/wpm-install-receipts-runtime 在处理；4 个 PS 环境项）；release 二进制 3.9MB。
- 权威数字来源：scripts/audit-command-compatibility.py 生成 DOCS/generated/（脚本输出已迁到 generated/，DOCS/en/ 旧副本已用生成结果覆盖）；EXEMPT.md 仍是旧口径（71/169, 98 exempt），未动，属遗留。
- 提交 c47c4c9 → 分支 docs/refresh-stats → PR #698（gh 认证走 niu -c "gh ..."，主系统 gh 未登录）。推送遇代理 TLS 断流，HTTP/1.1 这次有效。
- 统计刷新流程：audit 脚本生成矩阵 → baseline.json 数 case → ctest 实跑 → 改 README/banner/report。

## PR 合并收尾（2026-09-09 15:26）
- #390（wpm --proxy/--from/NO_PROXY）与 #698（README 统计刷新）均已 merge 到 main，远端 main = 564ff64。
- 本地 main 已 reset --hard 到 564ff64 对齐远端（本地分叉的 merge commit 内容全部上游化，无丢失）。两个 stash（其他会话的 format-only 遗留）保留未动，等归属会话处理。
- gh 合并/查询走 niu -c "gh ..."；merge 后 GitHub 的 mergeable 字段会短暂返回 UNKNOWN，等几秒再查。

## uutils 132 条 compat-candidate 差分终审（2026-09-09）
- 后台 close_noise.py 收口：552 条无效 issue 全部关闭（0 失败），仓库剩 ~191 有效集。
- 新脚本：K:/coreutils-issues/audit_candidates.py（82 用例差分）、retest_candidates.py、retest3.py（补测）、finalize_candidates.py（写池）、append_report.py（写报告）。
- 终审结果：132 → GAP-待修 24 / SAME 41 / 措辞差异 13 / 保留候选 5 / 排除 49。明细在 GAP_REPORT.md「候选筛选终审」章节，池内原因已同步 active_pool.json。
- 六大 GAP 族：A /dev/null|zero|stdin 伪设备参数不解析（7条）；B mkdir/install 长路径 >260 失败（3条，注意 rm/cp 读删列 150 层正常，仅创建路径受影响）；C date 小写t/军用j/时区缩写（4条）；D cp 递归遇悬空/父指向符号链接报错且复制不完整 + cp -l 不解引用（3条）；E df 不存在 operand 不报错 rc=0（2条）；F numfmt --grouping 忽略 locale、csplit --suppress-matched 末尾语义、chgrp 数字 gid、pr rc=0、od -tf2（5条）。
- 方法论坑：GNU 以 /usr/bin/xxx 全路径调用时错误前缀带路径，比较前必须做 argv0 归一化（否则 ~15% 假 DIFF）；msys 没有 more.exe，相关候选无法差分。
- 源码审查替代危险测试：rm --preserve-root 符号链接绕过（uutils#9705）——rm.cpp:457 递归条件排除 reparse point，无漏洞，不执行 rm -rf symlink-to-/。

## 仓库对账收口（2026-09-09）
- reconcile_repo.py 按终审结论对账：24 候选 GAP 转 compat-gap（标题 [candidate]→[gap]）、13 措辞差异转新标签 wording、103 条 SAME/排除/遗留关闭（not_planned）、5 条不可差分候选保留。
- 意外收获：遗留 unreviewed #933（yes 无效 unicode on Windows）差分实测是真 GAP——winuxcmd 把 \xff 转成 UTF-8（c3 bf）且管道输出混入 CRLF（0d 0a），GNU 原样 ff 0a。已转 compat-gap。
- 仓库终态：open 102 = 69 compat-gap + 13 wording + 8 i18n + 7 savannah + 5 compat-candidate，与"只发要修的"目标一致。

## 二轮全量复筛与发布（2026-09-09 下午）
- 用户纠正：不开修，只筛+发 issue；路径类问题先查 niubash 层。niubash 1.0.1 安装于 AppData\Local\Programs\Winuxsh（niu.exe），/dev 是物理目录映射 winuxcmd\dev\（dev/null 是 9KB HTML 打包事故，zero/stdin 缺失）；长路径 mkdir 在 niu 下同样失败。
- 二轮：760 条 open 非 PR 全量过筛（303 条此前规则桶未逐条看）→ 新 GAP 5 条（12790 ptx -W 正则、12676 TIME_STYLE、11380 长选项缩写、9795 关闭stdin、12518 sha3 缺失）、SAME 8、措辞 1、对齐9.x 1、待复测 1（7737 双横线超时）。
- 补发措辞差异 36 条（15 条误关重开 + 21 新发，label wording）；新发 GAP 5 条；8 条 /dev+长路径 issue 正文补 niubash 实测语境。
- 仓库终态：open 143 = compat-gap 74 + wording 49 + i18n 8 + savannah 7 + 保留候选 5。
- 工具坑：repo_uutils_map 的值 int 键 vs uutils 号 str 键比较，`in dict` 恒 False（publish_round2 的 body=0 事故）；GNU 8.32 的 date -f 目录静默 rc=0 是版本怪癖，别当差距。

## 三轮捞漏（2026-09-09 傍晚）
- gap_notes 清点补发 4 条（#975-978：date 相对偏移、date stat 风格、LS_COLORS、pr 多文件独立分页）——gap_notes 是 GNU 轮的跟踪键，之前一直没对到 repo，清算动作值得固化。
- closed 大盘捞漏：13059 条 closed → 排除已审计/已发布后 1861 条漏网 → 关键词 211 → 实测 36 → 新 GAP 9（#979-987）+ 措辞 4（#988-991）。uutils 修复历史 = 现成 bug 用例库，closed 捞漏产出率 9/36 远高于 open 池，以后每次 uutils 批量关闭后都值得扫一遍 closed。
- 新发现的系统性问题：realpath 不解析中间符号链接、chmod -v 无输出、md5sum 选项后置解析缺陷、printf %.2e 舍入。
- harness 教训：GNU/W 两侧必须独立 tmpdir（共用会 File exists 污染，本轮 7 条假 DIFF）。
- 仓库终态：open 160 = compat-gap 83 + 4 条无标签 gap（#975-978 新发漏带 uutils 标签）+ wording 53 + i18n 8 + savannah 7 + 保留候选 5。#975-978 下次顺手补 uutils-free 标签即可。
