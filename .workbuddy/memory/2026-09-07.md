
## 18:51 自动化任务：抓取 issue + 修复 winuxcmd
- 增量抓取 uutils（1000 open + 300 updated）与 GNU Savannah/NEWS 到 K:\coreutils-issues（fetch_incremental.py）。
- 修复 3 个 GNU 兼容 bug：echo -e 不展开 \u/\U；printf 畸形 \u/\U 报错退出 1；find -nogroup 匹配 Windows "None" 主组。测试 2361/2366（仅剩 5 个 ldd 平台限制）。
- i18n：legacy.4741b0a7dde8ac5a 补入 zh-CN（注意键是 FNV-1a 64bit，不是 sha1）。
- 提交并推送：wpm-alias-expansion 分支 7f47fe9/f9a237e；i18n 仓库 d1b5312。
- 教训：全套测试约 3-6 分钟，别当卡死；LNK1236 重试即可；format.py 约 7 分钟且会修无关文件的历史格式漂移，样式改动单独提交。

## 20:05 持续抓取任务
- uutils open 补全：双排序窗口（updated+created）各 1000 条合并去重 = 1049 条（636 issues + 413 PRs）；closed 近期 1000 条。GitHub 列表端点单窗口上限 1000（超限 422 属正常）。
- Savannah open bugs 全集仅 21 条（report_id=102），解析为 gnu/savannah_open_bugs.json；详情页因 python urllib 批量重试触发 IP 级 TLS 封禁（持续 >20 分钟），待自动化补抓。教训：Savannah 请求间隔 >=10s，失败立即停。
- 行为抽查（winuxcmd vs GNU）：shuf/test/tail/unexpand 的报错缺具体参数值（低优先批量改进项）；printf --、mv 大小写改名、tr/expand 多字节均一致。
- 产出 K:\coreutils-issues\summary\REVIEW_2026-09-07.md。
- 已建每日自动化"uutils-GNU-issue-持续抓取与复核"（id 74b616ac，每天 09:00，ACTIVE）。

## 21:35 自动化任务：抓取 issue + 修复 winuxcmd（第二轮）
- GitHub API 整轮 403 限流，Savannah TLS 封禁未解；本轮未新增 issue 数据，沿用既有 1000+ uutils open / Savannah 21 条。
- 新发现系统性缺口：`/d/...`（Git Bash/MSYS）风格路径只有走 make_api_path_operand 的命令（cat/ls/stat 等）能用，wc、校验和家族（md5sum~sha512sum/b2sum/cksum/sum）、rev/nl/fold/fmt/cut/paste/od/hexdump/grep/file/comm/cmp/dos2unix/d2u 共 20+ 命令直接 ifstream 打开全部失败。
- 修法：utils/native_path.cppm 的窄字符 normalize_api_operand 委托宽字符版做 `/x/... → X:\` 转换；各命令打开点包一层（digest.cppm 里 hash_file_hex 需 `import :native_path;`）。file.cpp 的 wpath 是 wstring，要用 _w 版本。
- 另修 base64/base32/basenc `-w 0` 多余尾随换行（GNU 无换行），5 处单测断言同步更新。
- 测试回到基线 2361/2366（仅剩 5 个 ldd 平台限制）。提交 cc1bc4f 已推送；本轮无新增用户可见文案，i18n 仓库无需改动。
- 新教训：python 脚本勿放 /tmp（Windows exe 解析成 d:\tmp）；bash heredoc 里的 \\n 会被转义处理，改用 Edit 工具改测试断言。

## 21:55 修复复核发现的全部差异
- 修 5 命令：shuf（报错带值+EOVERFLOW、-n 溢出钳制、范围非负+首个 - 分割）、tail（报错带值+EOVERFLOW）、expand/unexpand（GNU 消息全套+先解析后校验）、test（GNU posixtest ≤4 参数特殊形式+全套错误消息，单参 ! ( 为字符串测试）。uniq 复核已对齐无需改。
- i18n：新键走 translate_error 映射（i18n.cppm quoted/patterns/exact 表）+ zh-CN 11 键 + i18n_batch.py 内置字典登记。
- 坑1：i18n.cppm 内裸调 format() 会解析成 std::format，键名被当格式串原样返回——必须 ::winux::i18n::format 全限定。
- 坑2（严重）：会话期间另一并发会话把工作区回滚（丢失我全部未提交修改，含已编辑文件），靠重新应用+立即提交止损。教训：这个工作区可能有多会话并发，改动要尽早 commit。
- 坑3：find -anewer 测试因 NTFS 同刻度时间戳而 flaky，用 _wutime64 回档 ref 修复（b65d8b2）。
- 最终：2378 测试 2373 过（5 ldd 平台限制）。提交 19b6b41/7352c28/fd6b970/b65d8b2 已推送；i18n 仓库 4862c50 已推送。

## Run 3（自动化，~22:40）
- 刷新 K:\coreutils-issues：uutils open 到 #14433（新增 10 条）；GNU Savannah 仍超时，沿用旧数据。
- 修 tr [c*n]（66edee2，wpm-alias-expansion 已推送；i18n 仓库 13d9904 已推送）：[*]/[c*0] 之前按字面解析（tr 'l' '[x*]' 输出 he[[o），现按 GNU 语义做不定重复填充；补齐 GNU 三条校验报错；计数八进制解析/溢出报错；物化钳制 65536（对 256 项翻译表语义等价，GNU 对 1e12 计数自己都会卡死）。
- GNU 语义实测确认：repeat 展开按字符配对、后出现者胜出（tr '[a*3]b' 'xy' -> yyc），不是元素配对。
- chmod '4755,2' 拒绝是对的（GNU 也拒绝逗号分隔八进制模式），无需改。
- 坑4：串行 ctest 不带 </dev/null 会在 logger 测试(995)挂死（stdin 管道不关）；PowerShell 工具后台任务默认 120s 被杀，需显式 timeout；ctest -j8 会造成 tty/stty/sleep 假失败（终端资源竞争）。
- 最终 2377/2382（仅 5 个已知 ldd）。zh catalog 运行时路径是 exe 目录 build-vs/usr/bin/.wpm/i18n/zh-CN/，不是仓库根。
- i18n 残留：extract 输出与 zh-CN 之间仍有 ~150 个历史 key 漂移未处理。

## 2026-09-08 run 4 (~00:48, automation e1238a3c)
- Issue fetch: uutils refreshed (open_compact.json 1000 issues, newest #14437). Savannah GNU fetch still TLS/timeouts — existing gnu data reused.
- Verified winuxcmd ALREADY matches GNU on new issues: date --file on directory exits 1; chmod rejects octal-in-list and empty clauses (uutils #14434/#14435/#14421). No change needed.
- Fixed 2 real GNU-parity gaps (commit 8f1c09b on wpm-alias-expansion, pushed; i18n repo commit a055ad3 pushed):
  1. fold: obsolete -WIDTH form now accepted via dispatcher rewrite hook rewrite_fold_obsolete_args — rewrites -DIGITS to --width=N in place (last-one-wins), skips -w/--width values and args after -- (uutils #14248). Width errors now GNU-exact: "invalid number of columns: 'V'" / "Numerical result out of range" (0 or overflow).
  2. du/df: --block-size with BARE unit suffix (e.g. -BM, -B M) now appends the suffix to output values (du -BM -> "1M", df -BM -> "225207M", df header "1M-blocks" already existed); integer-prefixed specs (1M) scale silently (uutils #13605). Both parse_block_size copies (du.cpp, df.cpp) gained a display_suffix out-param; df has TWO print paths (print_size_columns AND format_block_value/output_cell) — both needed patching.
- i18n: new keys common.error.invalid_num_columns + common.error.numerical_range registered in i18n.cppm (quoted/exact tables), i18n_batch.py MANUAL_MESSAGES, zh-CN catalog (列数无效：'{}' / 数值结果超出范围). Runtime zh verified.
- Full suite: 2381/2386, only 5 known ldd Windows platform-limitation failures. 8 new unit tests (fold 5, du 2, df 1) all pass.
- Gotchas: after df/fold edits, remember df's dual print paths; TEST_LOG "Exit code: 1 (FAILED)" lines in test logs are expected-exit-code noise, not failures; parse FAILURES from gtest blocks by "(NN ms)   FAILED" preceded by RUN.
