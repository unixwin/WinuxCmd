# chcon 命令

## 状态

**Placeholder / Windows limitation**。WinuxCmd 注册并解析该命令以提供 GNU 兼容入口，但 Windows 不提供 SELinux 文件安全上下文，因此不会执行 GNU chcon 操作。

## 命令格式

`chcon [选项]... CONTEXT FILE...`


当前行为是报告平台不支持并以非零状态退出。不要将该命令计入行为兼容命令。
