interface:
  display_name: "WinuxCmd Workspace"
  short_description: "Repo-local WinuxCmd install and usage"
  default_prompt: "Use $winuxcmd to activate the repository-local WinuxCmd workspace or integrate a downloaded WinuxCmd build, then run WinuxCmd commands directly and prefer man.exe for help."

policy:
  allow_implicit_invocation: true
